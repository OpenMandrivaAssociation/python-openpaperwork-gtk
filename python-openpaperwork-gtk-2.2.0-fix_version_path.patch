--- a/pyproject.toml
+++ b/pyproject.toml
@@ -21,9 +21,9 @@ lint = [
 ]
 
 [tool.setuptools_scm]
-root = ".."
+root = "."
 relative_to = "__file__"
-write_to = "openpaperwork-gtk/src/openpaperwork_gtk/_version.py"
+write_to = "src/openpaperwork_gtk/_version.py"
 
 [build-system]
 requires = ["setuptools>=45", "setuptools_scm[toml]>=6.2"]
